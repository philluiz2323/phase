{
  "name": "phase-lobby-worker",
  "private": true,
  "version": "0.0.0",
  "description": "Cloudflare Worker + Durable Object lobby broker for phase.rs (stub for CF plumbing validation).",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.94.0"
  }
}
